{
  "name": "nest-angular-ionic-starter",
  "version": "1.0.0",
  "scripts": {
    "start": "cd client-web && ng build --prod && cd ../server && npm run build && mv ../client-web/dist/* ../server/dist/client-web && cd .. && npm run start:prod"
  }
}
