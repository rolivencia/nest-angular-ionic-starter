{
  "name": "nest-angular-ionic-starter",
  "version": "1.0.0",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "setup-client": "cd client-web && npm install",
    "setup-server": "cd server && npm install",
    "heroku-prebuild": "npm run setup-client && npm run setup-server",
    "build": "mv ../client-web/dist/* ../server/dist/client-web",
    "start": "cd .. && npm run start:prod"
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
